{
  "name": "pm-odyssey",
  "version": "1.0.0",
  "description": "Jackie Hou's Product Manager Portfolio",
  "scripts": {
    "start": "python scripts/preview.py",
    "deploy": "gh-pages -d .",
    "build": "echo 'No build required for static site'",
    "generate:redirects": "node scripts/generate-legacy-redirects.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
